<template>
  <div class="app-container">
    <count-to
      ref="example"
      :start-val="_startVal"
      :end-val="_endVal"
      :duration="_duration"
      :decimals="_decimals"
      :separator="_separator"
      :prefix="_prefix"
      :suffix="_suffix"
      :autoplay="false"
      class="example"
    />

    <div style="margin-left: 25%; margin-top: 40px">
      <label class="label" for="startValInput">
        startVal:
        <input
          v-model.number="setStartVal"
          type="number"
          name="startValInput"
        />
      </label>
      <label class="label" for="endValInput">
        endVal:
        <input v-model.number="setEndVal" type="number" name="endVaInput" />
      </label>
      <label class="label" for="durationInput">
        duration:
        <input
          v-model.number="setDuration"
          type="number"
          name="durationInput"
        />
      </label>
      <div class="startBtn example-btn" @click="start">Start</div>
      <div class="pause-resume-btn example-btn" @click="pauseResume">
        pause/resume
      </div>
      <br />
      <label class="label" for="decimalsInput">
        decimals:
        <input
          v-model.number="setDecimals"
          type="number"
          name="decimalsInput"
        />
      </label>
      <label class="label" for="separatorInput">
        separator:
        <input v-model="setSeparator" name="separatorInput" />
      </label>
      <label class="label" for="prefixInput">
        prefix:
        <input v-model="setPrefix" name="prefixInput" />
      </label>
      <label class="label" for="suffixInput">
        suffix:
        <input v-model="setSuffix" name="suffixInput" />
      </label>
    </div>
  </div>
</template>

<script setup name="CountToDemo">
import CountTo from '../../components/CountTo/index.vue'

const data = reactive({
  setStartVal: 0,
  setEndVal: 2017,
  setDuration: 4000,
  setDecimals: 0,
  setSeparator: ',',
  setSuffix: ' rmb',
  setPrefix: '¥ '
})

const {
  setStartVal,
  setEndVal,
  setDuration,
  setDecimals,
  setSeparator,
  setSuffix,
  setPrefix
} = toRefs(data)

const example = ref(null)

const start = () => {
  example.value.start()
}

const pauseResume = () => {
  example.value.pauseResume()
}

const _startVal = computed(() => {
  if (data.setStartVal) {
    return data.setStartVal
  } else {
    return 0
  }
})

const _endVal = computed(() => {
  if (data.setEndVal) {
    return data.setEndVal
  } else {
    return 0
  }
})
const _duration = computed(() => {
  if (data.setDuration) {
    return data.setDuration
  } else {
    return 100
  }
})
const _decimals = computed(() => {
  if (data.setDecimals) {
    if (data.setDecimals < 0 || data.setDecimals > 20) {
      alert('digits argument must be between 0 and 20')
      return 0
    }
    return data.setDecimals
  } else {
    return 0
  }
})

const _separator = computed(() => {
  return data.setSeparator
})

const _suffix = computed(() => {
  return data.setSuffix
})

const _prefix = computed(() => {
  return data.setPrefix
})
</script>

<style scoped>
.example-btn {
  display: inline-block;
  margin-bottom: 0;
  font-weight: 500;
  text-align: center;
  -ms-touch-action: manipulation;
  touch-action: manipulation;
  cursor: pointer;
  background-image: none;
  border: 1px solid transparent;
  white-space: nowrap;
  line-height: 1.5;
  padding: 4px 15px;
  font-size: 12px;
  border-radius: 4px;
  -webkit-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
  user-select: none;
  -webkit-transition: all 0.3s cubic-bezier(0.645, 0.045, 0.355, 1);
  transition: all 0.3s cubic-bezier(0.645, 0.045, 0.355, 1);
  position: relative;
  color: rgba(0, 0, 0, 0.65);
  background-color: #fff;
  border-color: #d9d9d9;
}

.example-btn:hover {
  color: #4ab7bd;
  background-color: #fff;
  border-color: #4ab7bd;
}
.example {
  font-size: 50px;
  color: #f6416c;
  display: block;
  margin: 10px 0;
  text-align: center;
  font-size: 80px;
  font-weight: 500;
}

.label {
  color: #2f4f4f;
  font-size: 16px;
  display: inline-block;
  margin: 15px 30px 15px 0;
}

input {
  position: relative;
  display: inline-block;
  padding: 4px 7px;
  width: 70px;
  height: 28px;
  cursor: text;
  font-size: 12px;
  line-height: 1.5;
  color: rgba(0, 0, 0, 0.65);
  background-color: #fff;
  background-image: none;
  border: 1px solid #d9d9d9;
  border-radius: 4px;
  -webkit-transition: all 0.3s;
  transition: all 0.3s;
}

.startBtn {
  margin-left: 20px;
  font-size: 20px;
  color: #30b08f;
  background-color: #fff;
}

.startBtn:hover {
  background-color: #30b08f;
  color: #fff;
  border-color: #30b08f;
}

.pause-resume-btn {
  font-size: 20px;
  color: #e65d6e;
  background-color: #fff;
}

.pause-resume-btn:hover {
  background-color: #e65d6e;
  color: #fff;
  border-color: #e65d6e;
}
</style>
